---
name: Bump Version
description: Bump a Python project version using Commitizen and release a new version in GitHub.

inputs:
  machine-user-pat:
    description: Github Catena Machine User Personal Access Token (PAT)
    required: true

runs:
  using: docker
  image: Dockerfile
